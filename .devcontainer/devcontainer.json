{
    "name": "Docker Compose",
    "dockerComposeFile": ["docker-compose.yml"],
    "service": "fastapi", // Specify the service you want to start (change to the desired service name)
    "workspaceFolder": "/workspace",
    "extensions": [],
    "postCreateCommand": "docker-compose build", // Command to run after the container is created
    "runArgs": ["up"], // Arguments to pass to the 'docker-compose up' command
    "runCommand": "docker-compose",
    "settings": {}
  }
  
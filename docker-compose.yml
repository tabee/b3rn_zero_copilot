version: '3.8'
services:
  fastapi:
    build: ./fastapi-container
    ports:
      - "8000:80"
  langchain:
    build: ./langchain-container
    environment:
      - OPENAI_API_KEY=dein_api_schlüssel
